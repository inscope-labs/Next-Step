// Package taskseq implements the sequential task counter for a workspace.
//
// Workspace-scoped from day one — this carries forward the fix that was
// applied as a patch (v1.2.1) in the retired ABX-STEP lineage, rather than
// reintroducing the hardcoded/global-scope bug and patching it later.
//
// Phase 4 stub: package skeleton only, no logic yet. Built out in Phase 5
// per the Next Step build plan.
package taskseq
