#!/usr/bin/env bash
# Placeholder — install.sh
# To be implemented in Phase 8 (Public install flow).
# Will: detect platform, fetch matching `next-step` release binary,
# verify checksum, place binary + root/ payload at $NEXT_STEP_HOME.
set -euo pipefail
echo "next-step install.sh: not yet implemented (Phase 8)"
exit 1
