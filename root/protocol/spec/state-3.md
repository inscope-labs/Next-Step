# STATE 3 — Confirmation

No commands. Just confirm and stop.

```
ONBOARDED. install=<path from State 0> auth=human-only demo_verified=<yes/no>
```

Onboarding is done. Normal task work follows the authorization model above —
you propose, the human authorizes, the runner executes and verifies.
